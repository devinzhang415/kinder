#ifndef UTIL_HPP
#define UTIL_HPP

#include <iostream>

enum Player {
    _ = '_', // empty space
    X = 'X', // X player (goes first)
    O = 'O', // O player
    // these enums are set to certain char values for ease of printing
};

#endif
